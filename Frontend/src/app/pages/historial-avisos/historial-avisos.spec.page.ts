/* import { ComponentFixture, TestBed, waitForAsync } from '@angular/core/testing';

import { HistorialAvisosComponent } from './historial-avisos.page';

describe('HistorialAvisosComponent', () => {
  let component: HistorialAvisosComponent;
  let fixture: ComponentFixture<HistorialAvisosComponent>;

  beforeEach(waitForAsync(() => {
    TestBed.configureTestingModule({
      imports: [HistorialAvisosComponent],
    }).compileComponents();

    fixture = TestBed.createComponent(HistorialAvisosComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */